name: Deploy on Merge - DISABLED

on:
  push:
    branches: [ main, production ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ false }}  # Always skip this workflow
    steps:
      - name: Workflow Disabled
        run: echo "This workflow is disabled. Use fast-deploy.yml instead"